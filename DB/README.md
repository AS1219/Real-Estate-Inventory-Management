# Database Folder

This folder contains the SQLite schema for the Real Estate Inventory Management app.

- `schema.sql` — database schema definitions for projects, buildings, flats, activities, and analytics data.
- `realestate.db` — created when the backend database initialization script runs.

Use the backend scripts to initialize and seed the database:

```bash
cd backend
npm run db:init
```
